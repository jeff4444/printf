#include "main.h"
/**
 * print_pointer - prints an address contained by
 * a pointer
 * @args: list of cmd line arguments
 * @buffer: buffer
 * @flags: flags
 * @width: width
 * @precision: precision
 * @size: size
 *
 * Return: number of printed characters
 */
int print_pointer(va_list args, char buffer[], int flags,
		int width, int precision, int size)
{
	return (1);
}
